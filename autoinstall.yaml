autoinstall:
  version: 1
  identity:
    hostname: ubuntu-desktop
    username: local-optimum
    password: "$6$ZE4WV3QRJhPUnsNv$BpkTBYjUOxOiWV5sNPYDSitTwxW.3NHLmhRqptzpa8a4KTxGpkvMaSDbyq4PVri9kdpD1t7ldUBgwB6uveObg."
  storage:
    layout:
      name: lvm
  snaps:
    - name: spotify
      classic: false
    - name: telegram-desktop
      classic: false
    - name: obsidian
      classic: true
    - name: code
      classic: true
  packages:
    - vim
  late-commands:
    - "wget -O /tmp/protonvpn.deb https://repo2.protonvpn.com/debian/dists/stable/main/binary-all/protonvpn-stable-release_1.0.3-3_all.deb"
    - "dpkg -i /tmp/protonvpn.deb"
    - "apt-get update"
    - "apt-get install -y protonvpn"
    - "systemctl enable protonvpn.service"
    - "systemctl start protonvpn.service"
    - 'echo "ProtonVPN installation completed" >> /var/log/installation.log'

